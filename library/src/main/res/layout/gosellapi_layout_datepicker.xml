<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layoutDirection="ltr"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="10dp">

   <!-- <LinearLayout
        android:layout_height="wrap_content"
        android:layout_width="0dp"
        android:orientation="vertical"
        android:layout_weight="1"
        android:paddingStart="@dimen/margin_basic_10dp"
        android:gravity="center"
        tools:ignore="RtlSymmetry"
        android:paddingLeft="@dimen/margin_basic_10dp">
        <TextView
            android:id="@+id/tvMonth"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginBottom="10dp"/>

        <com.aigestudio.wheelpicker.WheelPicker
            android:id="@+id/npMonthPicker"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:wheel_cyclic="true"
            app:wheel_curved="true"
            app:wheel_atmospheric="true"
            app:wheel_indicator="true"
            app:wheel_indicator_color="@color/aluminium"
            app:wheel_indicator_size="2px"
            app:wheel_item_text_size="17sp"
            app:wheel_item_text_color="@color/oslo_gray"
            app:wheel_selected_item_text_color="@android:color/black"
            app:wheel_item_space="20dp"
            android:layout_gravity="bottom"
            tools:ignore="PxUsage"/>

    </LinearLayout>
    <LinearLayout
        android:layout_height="wrap_content"
        android:layout_width="0dp"
        android:orientation="vertical"
        android:layout_weight="1"
        android:paddingEnd="@dimen/margin_basic_10dp"
        android:gravity="center"
        tools:ignore="RtlSymmetry"
        android:paddingRight="@dimen/margin_basic_10dp">
        <TextView
            android:id="@+id/tvYear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginBottom="10dp" />
        <com.aigestudio.wheelpicker.WheelPicker
            android:id="@+id/npYearPicker"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:wheel_cyclic="true"
            app:wheel_curved="true"
            app:wheel_atmospheric="true"
            app:wheel_indicator="true"
            app:wheel_indicator_color="@color/aluminium"
            app:wheel_indicator_size="2px"
            app:wheel_item_text_size="17sp"
            app:wheel_item_text_color="@color/oslo_gray"
            app:wheel_selected_item_text_color="@android:color/black"
            app:wheel_item_space="20dp"
            android:layout_gravity="bottom"
            tools:ignore="PxUsage"/>
    </LinearLayout>-->
</LinearLayout>